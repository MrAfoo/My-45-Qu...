// Create an array of your favorite pizza types
const favoritePizzas = ['pepperoni', 'Fajita', 'creamy mushroom'];

// Print the name of each pizza
for (let i = 0; i < favoritePizzas.length; i++) {
    console.log(favoritePizzas[i]);
}

// Modify the loop to print a sentence for each pizza
for (let i = 0; i < favoritePizzas.length; i++) {
    console.log(`I like ${favoritePizzas[i]} pizza.`);
}

// Add a line outside the loop expressing your love for pizza
console.log("I really love pizza!");

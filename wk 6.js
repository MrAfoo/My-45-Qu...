let a = 5;         //Declaring values to Variables
let b = 3;
let sum = a + b;   //Variables Adding Method
console.log(`The sum of ${a} and ${b} is: ${sum}`);  //Printing Addition of "a" and "b"

let c = 10;         //Declaring values to Variables
let d = 2;
let sub = c - d    //Variables Subtracting Method
console.log(`The subtract of ${a} and ${b} is: ${sub}`);  //Printing subtraction of "a" and "b"

let e = 4;         //Declaring values to Variables
let f = 2;
let mul = e * f    //Variables Multiplying Method
console.log(`The multiply of ${a} and ${b} is: ${mul}`);  //Printing multiplication of "a" and "b"

let g = 16;         //Declaring values to Variables
let h = 2;
let div = g / h     //Variables Dividing  Method
console.log(`The division of ${a} and ${b} is: ${div}`);  //Printing division of "a" and "b"

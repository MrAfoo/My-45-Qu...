// Create an array of friends' names
const names = ["Azaan", "Anas", "Muneeb", "Usman"];

// Print a personalized message for each friend
for (let i = 0; i < names.length; i++) {
    console.log(`Hello, ${names[i]}! You're an awesome friend.`);
}

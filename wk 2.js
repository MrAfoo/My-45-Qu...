// Store the person's name in a variable
let x = "Affan";

// Print the name in lowercase, uppercase, and titlecase
console.log(`x in lowercase: ${x.toLowerCase()}`);
console.log(`x in uppercase: ${x.toUpperCase()}`);
console.log(`x in titlecase: ${x.charAt(0).toUpperCase() + x.slice(1).toLowerCase()}`);

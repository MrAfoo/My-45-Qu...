// Create an array of friends' names
let names = ["Azaan", "Usman", "Anas", "Muneeb"];

// Print each person's name
for (let i = 0; i < names.length; i++) {
    console.log(`Friend ${i + 1}: ${names[i]}`);
}
